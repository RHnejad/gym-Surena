from setuptools import setup

setup(
    name="Surena",
    version='0.0.1',
    install_requires=['gym',
                      'pybullet',
                      'numpy',
                      'time',
                      'pybullet_data']
)
